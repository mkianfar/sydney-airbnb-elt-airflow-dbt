# Local run (scripts/backfill.py, dbt, tests). Airflow itself runs on Cloud Composer and is not needed here.
dbt-postgres>=1.9,<2   # dbt-core 1.10+ is required (tests use the `arguments:` property)
psycopg2-binary>=2.9
pytest>=8
